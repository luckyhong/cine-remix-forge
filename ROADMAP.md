# 创意与功能路线图

这是一份持续累积的想法清单，不是承诺的开发计划——记下来方便随时翻、方便以后接着加新点子，想认真做哪个再单独拉出来立项。每条尽量写清楚"是什么、为什么值得做、大概要多少成本/前提条件"，方便挑选时不用重新想一遍。

## 已经做完的（避免重复提议）

这几个是之前讨论过、也已经落地的，别再当新点子提出来了，直接看对应文档：

- **动画短片渲染成视频**（手绘风 Remotion / 即梦-Seedance 可粘贴 prompt）→ [`references/video_render_routes.md`](references/video_render_routes.md)
- **文档存放位置用户可选** → `AGENTS.md` 第七步
- **多工具支持（Claude Code/Cursor/Codex/纯对话模型）** → `AGENTS.md` + 各工具适配文件
- **动画载体意象去重记录** → [`references/used_concepts_log.md`](references/used_concepts_log.md)

## 内容深度与风格

### 解说台词风格库
现在的解说词只有一种"腔调"（严肃反转式论证）。可以做一份 `references/narration_style_library.md`，列几种可选人设——学术严谨型、犀利吐槽型、温情人文型、悬疑节奏型、玩梗年轻化型——每种给出具体的措辞习惯、转折句式偏好、开篇钩子倾向，让用户在设计论点那一步顺带选风格，而不是每次都是同一种调调。

**成本**：低。参考已有的 `style_keyword_mapping.md` 做法照搬一份就行，不涉及新的技术依赖。

### 多语言支持（不止英文）
英文是最直接的扩展，但真正做起来不是加个翻译开关：
- 现有的分幕格式、钩子句式、转折词表都是中文语境设计的，英文版需要一套独立的英文钩子/收尾句式库，不能机翻现有中文示例
- 路线一（`story-to-handdrawn-video`）字数上限按字符数卡（45字符），中文一个字信息量大，英文45字符可能就7-8个单词，需要单独设计按单词数/语义单元切分的规则

除了英文，日语/韩语/西语这些语区对中国经典电影解说也有真实需求（尤其老电影自带的历史背景对海外观众是陌生的）——如果做多语言，可以再加一层"文化注脚"：遇到需要背景知识才能懂的梗，自动加一句简短的文化背景说明，这是纯翻译版本不会自然具备的价值。

**成本**：中高。核心方法论可以复用，但每种语言的钩子/切分规则需要单独设计，不是一次性工作量。

## 剪辑与制作衔接

### 精准对应原片时间段，辅助剪辑
用户原始诉求：解说的每一段最好能标出"这对应原片大概第几分钟"，方便真人剪辑取素材。

**重要边界，必须诚实**：不能凭空给出"原片第几分钟是哪场戏"这种具体数字——没有可靠依据的话，编出来就是 `AGENTS.md` 里一直在防的"编造具体细节"。但在有素材的前提下是可以做实的：

- **用户提供原片字幕文件（SRT/VTT）**：字幕自带真实时间戳，可以写脚本把解说词提到的情节点和字幕内容做文本相似度匹配，反推"这段解说对应字幕里大概第几分钟的哪几句台词"——时间戳是真实的，因为源头是字幕文件本身，不是模型编的
- **用户提供原片视频文件**：用 `ffmpeg`/`pyscenedetect` 做镜头切分检测，得到真实的镜头时间轴，再用视觉模型分析抽帧画面去匹配解说词描述的场景——更重（要处理视频文件、可能需要视觉模型分析截图），但产出会更接近一份真正能用的剪辑点位表（EDL）

这两条路都要求用户提供额外素材，不能只靠"这是哪部电影"这一句话就变出时间戳——功能说明里要把这个前提写清楚，避免用户误以为随便说个片名就能拿到精确剪辑点。

**成本**：中（字幕匹配路线）到高（视频镜头检测路线）。字幕匹配路线优先做，性价比更高。

### 封面图 / 关键帧图 AI 出图 prompt
不只是"生成一张好看的图"——可以借用差异化策略思路，每次给2-3个风格差异明显的封面概念，每个配一份详细的出图 prompt（不是图片本身，因为这个 skill 本身不直接生成图片）。也可以顺带给动画短片剧本的分镜关键帧补一层"每场戏的关键帧 prompt"，让分镜脚本从"导演能看懂"升级到"能直接丢给生图工具跑"。

**成本**：低。纯文本生成，写法上和路线二（`screenplay_to_video_prompts.py`）是同一类工作。

### 配乐/音效结构化产出
现在解说词里的【音效】只是提示性文字，可以做成真正结构化的"配乐点位表"（时间点 + 情绪曲线 + 建议的乐器/风格关键词），对接 Suno 这类文生音乐工具——跟路线二给即梦/Seedance生成提示词是同一个思路，只是换成音乐赛道。

**成本**：低到中。产出形式和路线二类似，主要工作量在设计"情绪曲线怎么标注"这套规范。

### 声音人设 / TTS 虚拟解说员
结合 ElevenLabs、CosyVoice 这类 TTS 服务，给账号定一个固定的"虚拟解说员声音"，解说词生成完直接配出一版音频草稿。动画短片那条线是"静音，配音留给用户后期"的设计，但解说词这条线本来就是要真人/虚拟配音的，可以单独往这个方向走，不冲突。

**成本**：中。需要接入具体的 TTS 服务（本机目前没有现成的 MCP/API 接入，类似即梦/Seedance 那个调研结论），第一阶段可以先做"配音脚本+情绪/语速标注"的文本产出，不做真实调用。

## 质量与可靠性

### 多智能体流水线
现在是"一个模型从头写到尾"，可以拆成分工明确的几个角色接力：一个负责论点/立论，一个专门做"事实核查"（只干一件事——把解说词里涉及的历史/传记细节挑出来复核，不确定就打回去改措辞），一个专门写动画剧本，最后一个"总编辑"角色做统稿和版权自查。这样"不编造史实"这条红线会从"提醒模型注意"变成"有独立角色专门把关"，可靠性有质的提升。

**成本**：中高。需要重新设计工作流的编排方式（多次调用/多个角色），不是简单改一份参考文档就行。

### 版权自查层
输出前加一道自动比对——生成的解说词/动画剧本和一批已知的公开解说文案做文本相似度检测，进一步降低"无意间写得太像"的风险，而不是只靠事前的红线提醒。

**成本**：中。需要一个参照文案库和相似度检测逻辑，不需要复杂的模型，但需要维护一个比对数据源。

### 自我校验环节
生成完之后加一道自查——时长是否真的够、钩子是否够硬、首尾呼应是否真的落地了——按 `AGENTS.md` 里的硬性要求逐条打勾，而不是全靠模型自觉。

**成本**：低。可以做成一份检查清单让模型自己过一遍，不需要额外工具。

## 规模化与系列化

### 系列 / 合集模式
比如"一个导演的全部作品"批量生成，还能刻意设计跨集的呼应（这集埋的钩子下集回收），做出真正的系列感，而不是一堆孤立单集。

**成本**：中。核心生成逻辑不变，主要是加一层"跨集状态记忆"（类似 `used_concepts_log.md` 但记录的是论点角度和跨集伏笔）。

### 短视频切条
从长解说自动衍生出几个30-60秒的"钩子切片"文案，适配抖音/视频号这类短平台的二次分发。

**成本**：低。纯文本再加工，类似把已有内容按新规则重新剪裁。

### 论点去重日志
现在 `used_concepts_log.md` 只记录动画短片的"载体意象"，解说词的"核心论点角度"其实也该有一份类似记录，避免以后写多了论点也开始重复。

**成本**：低，是现有机制的直接扩展。

### 创作者画像 / 长期偏好记忆
扩展现有的"去重记录"机制，记录用户偏好的解说语气、常用论点角度、常选的画风——不是真的做模型微调，是 prompt 层面的"越用越懂你"。

**成本**：低到中。是对上面"论点去重日志"和"风格库"两个想法的进一步整合。

### catalog 自动索引
`cine-remix-catalog/works/` 攒多了之后，自动扫描生成一份目录页（片名/核心论点一句话/生成日期/用的动画意象），方便回顾。

**成本**：低。一个扫描 `works/` 目录、解析已有文档标题信息的小脚本就够。

## 呈现与分发

### 交互式网页产出
生成完的文档除了存成 `.md`，也可以顺手发布成一个可以在浏览器里"时间轴对照文案"查看的交互页面，校对时比翻 markdown 文件顺手很多——可以直接复用 Artifact 能力，不需要额外基础设施。

**成本**：低。产出内容不变，只是多一层展示格式。

### 平台格式适配
导出提词器友好的纯文本版、按时间轴生成 SRT 字幕骨架，方便不同发布场景直接用。

**成本**：低，是现有内容的格式转换。

---

## 优先级参考（个人判断，不是定论）

- **性价比最高，建议优先**：解说风格库、自我校验环节、论点去重日志、catalog自动索引——都是对现有机制的直接扩展，成本低
- **有新意但依赖外部条件**：字幕时间戳匹配（需要用户提供字幕/视频）、TTS配音（需要接入外部服务）
- **工作量大但方向扎实**：多语言支持、多智能体流水线——值得做，但不是一次能搞定的
